#include<iostream>
using namespace std;
void MD(int npgs,int from,int to,int temp)
{
	if(n<=0)
	{
		return;
	}
	else
	{
		MD(npgs-1,from,temp,to);
		cout<<"peg moved from "<<from<<" to"<< to<<endl;
		MD(npgs-1,temp,to,from);
	}
}
int main ()
{
	MD(3,1,3,2);
}

